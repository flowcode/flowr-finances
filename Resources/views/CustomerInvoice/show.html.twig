{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}CustomerInvoice - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            <a href="{{ path('finance_customer_invoice') }}"> {{ 'CustomerInvoice list'|trans({'CustomerInvoice': 'CustomerInvoice'}, 'CustomerInvoice') }}</a>
            /
            {{ 'CustomerInvoice' | trans({}, 'CustomerInvoice')}}
        </h1>
    </section>
    <section class="content">    
        <div class="box"  id='showCustomerInvoice'>
            <div class="box-header">
                <div class="box-tools  pull-right">
                            {{ form_start(delete_form) }}
        {{ form_widget(delete_form) }}

        <div class="btn-group record_actions">            <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'CustomerInvoice') }}</button>
            <input name="modal" type="hidden" value="0">
            
                <a class="btn btn-link  btn-sm" onclick="showEdit(true)">
                    {{ 'Edit'|trans({}, 'CustomerInvoice') }}
                </a>
</div>
            <input name="modal" type="hidden" value="0">
        {{ form_end(delete_form) }}

</div>
            </div>
            <div class="box-body">
                <table class="table table-hover record_properties">
                    <tbody>                            
                        <tr>
                            <th scope="row">{{'Created'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.created|date('Y-m-d H:i:s') }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Updated'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.updated|date('Y-m-d H:i:s') }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Total'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.total }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Totalwithtax'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.totalWithTax }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Discount'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.discount }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Totaldiscount'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.totalDiscount }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Tax'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.tax }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Status'|trans({}, 'CustomerInvoice')}}</th>
                            <td>{{ customerinvoice.status }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="box-footer">
            
            </div>
        </div>
        <div class="box" id='editFormCustomerInvoice'  style="display: none;">
            <div class="box-header">
                <div class="box-tools">
                    {{ form_start(delete_form) }}
                    {{ form_widget(delete_form) }}
                    <div class="btn-group record_actions">
                        <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'CustomerInvoice') }}</button>
                        <a class="btn btn-link  btn-sm" onclick="showEdit(false)">
                        {{ 'Cancel'|trans({}, 'Account') }}
                        </a>
                    </div>
                    <input name="modal" type="hidden" value="0">
                    {{ form_end(delete_form) }}
                </div>
            </div>
            <div class="box-body">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn-default btn" type="submit">{{ 'save'|trans | capitalize}}</button>
                </p>
            {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
            </div>
        </div>
    </section>

        <div class="btn-group record_actions"></div>

        <div id="delete_confirm" class="modal fade">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                        <h3>{{ 'Confirm delete'|trans({}, 'CustomerInvoice') }}</h3>
                    </div>
                    <div class="modal-body">
                        <p>{{ 'You are about to delete an item'|trans({}, 'CustomerInvoice') }}, {{ 'this procedure is irreversible'|trans({}, 'CustomerInvoice') }}.</p>
                        <p>{{ 'Do you want to proceed?'|trans({}, 'CustomerInvoice') }}</p>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-danger">{{ 'Yes'|trans({}, 'CustomerInvoice') }}</button>
                        <button class="btn btn-default" data-dismiss="modal">{{ 'No'|trans({}, 'CustomerInvoice') }}</button>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
{% block javascripts %}

    {{ parent() }}
    <script type="text/javascript">
        function showEdit(show){
            if(show){
                $('#showCustomerInvoice').hide();
                $('#editFormCustomerInvoice').show();
            }else{
                $('#showCustomerInvoice').show();
                $('#editFormCustomerInvoice').hide();
            }
        }
        
    </script>
{% endblock %}